information#this-page-info(data-type="tag")

.gm-title
    h1= `标签 ${page.tag} 下的文章`
    .desc.t-1.fs-sm.c-t2= `该标签下共有${page.posts.length}篇文章`

.arts-wrap.content-area 
    include _partial/show-posts.pug

include _partial/pagination.pug